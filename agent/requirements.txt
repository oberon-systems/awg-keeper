# Runtime dependencies of the agent. Pinned: the artifact is an rpm and a deb
# built in two different containers, and an unpinned rebuild would ship two
# different services under one version.
fastapi==0.141.1
pydantic==2.13.5
pydantic-settings==2.15.0
uvicorn==0.53.0
