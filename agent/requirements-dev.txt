# Test-only dependencies. The suite mocks awg and xray with fake binaries on
# PATH, so neither the tunnel nor root is the price of running it.
# fastapi's TestClient is httpx underneath.
httpx==0.28.1
pytest==9.1.1
