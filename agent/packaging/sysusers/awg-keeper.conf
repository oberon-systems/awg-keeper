# A system user with no shell and no home: the service reads its state from
# /var/lib/awg-keeper and its configuration from /etc/awg-keeper.
u awgkeeper - "awg-keeper agent" /var/lib/awg-keeper /usr/sbin/nologin
